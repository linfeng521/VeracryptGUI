<Window x:Class="VeracryptGui.CreateDiskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="创建加密磁盘" Height="400" Width="300" ResizeMode="NoResize">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="磁盘名称：" Margin="20,0,10,0" Width="80"></Label>
                <TextBox x:Name="createDiskName"  Margin="20,0,20,0" Width="150" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="密码：" Margin="20,0,30,0" Width="80"></Label>
                <PasswordBox Width="140" x:Name="createDiskPassword" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="大小：" Margin="20,0,30,0" Width="80"></Label>
                <TextBox Width="140" x:Name="diskSize" />
            </StackPanel>

            <Button x:Name="CreateDiskBtn" Content="创建磁盘" Click="CreateDiskBtn_Click" Margin="20" Width="55" />
        </StackPanel>
    </Grid>
</Window>